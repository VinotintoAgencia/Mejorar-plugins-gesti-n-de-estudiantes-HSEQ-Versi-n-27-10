# Vendor dependencies

The Composer dependencies for this plugin (notably `mpdf/mpdf`) could not be installed in this environment because outbound access to GitHub/Packagist is blocked (403 during download). To populate this directory, run `composer install --no-dev` in an environment with internet access and commit the resulting vendor files.
